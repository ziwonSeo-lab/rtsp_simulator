# 설치: uv pip sync requirements.txt  (또는 pip install -r requirements.txt)

# === 필수(core) 패키지 ===
# 운영에 항상 필요한 최소 의존성만 포함합니다.
opencv-python>=4.8.0
Pillow>=10.0.0
numpy>=1.24.0
psutil>=5.9.0
python-dotenv>=1.0.0
requests>=2.31.0
watchdog>=3.0.0
# 블러/추론 필수 (YOLO)
ultralytics>=8.0.0
# 기본 CPU 백엔드(필요 시 GPU용으로 교체 설치)
torch>=2.5.0
torchvision>=0.20.0

# === 선택(optional) 패키지 ===
# 아래 항목들은 블러/추론/가속 등을 사용할 때만 주석을 해제하고 설치하세요.
# 일부는 빌드 도구(cmake 등)가 필요하거나 GPU 환경이 필요합니다.

# GPU 모니터링 (NVIDIA GPU가 있는 경우)
# GPUtil>=1.4.0

# YOLO/추론 관련 (필요 시 대체)
# onnxruntime-gpu>=1.16.0   # 또는 CPU용 onnxruntime>=1.16.0
# onnxsim==0.4.33           # 소스 빌드 필요. cmake/ninja 등이 시스템에 설치되어야 함
# scikit-image>=0.21.0      # 고급 이미지 처리(선택)
# matplotlib>=3.7.0         # 시각화/디버깅(선택)

# === 참고: CUDA 12.1 + TensorRT 설치 가이드(선택) ===
# 1) PyTorch CUDA 12.1 (공식 호환표 기준)
#    uv pip install torch==2.5.1+cu121 torchvision==0.20.1+cu121 \
#        --index-url https://download.pytorch.org/whl/cu121
# 2) 시스템 의존성(onnxsim 빌드용)
#    sudo apt-get install -y cmake ninja-build build-essential git python3-dev
# 3) TensorRT (NVIDIA PyPI 레지스트리)
#    uv pip install tensorrt-cu12==10.0.1 tensorrt-cu12-bindings==10.0.1 \
#        tensorrt-cu12-libs==10.0.1 --extra-index-url https://pypi.ngc.nvidia.com

# === 시스템 요구사항(요약) ===
# - Python >= 3.8
# - FFmpeg (별도 설치 필요): sudo apt-get install -y ffmpeg
# - (선택) NVIDIA GPU, CUDA/TensorRT, cmake/ninja 등 빌드 도구
